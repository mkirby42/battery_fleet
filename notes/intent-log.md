# Intent log

Decisions and reasoning in the author's words. This is raw material for a future post. Agents do not write that post.

After each design call: add a dated entry, quote the author, then one line for what we locked.

---

## 2026-08-23 — Backbone, not the whole spec

> "This spec is a bit expansive for the initial buildout. I'd like to build the sim backbone."

> "The existing spec gets the sim logic pretty right, but misses the importantce of the underlying data model to the resilliance of the sim."

> "We should use concepts from verification, validation, and uncertianty quantification (VVUQ) to make sure each sim run and battery model, market model, etc is handled in a understandable mannor and we dont just plop out masses of code and documentation that will never be seen or read."

> "Special emphasis shuld be placed on making the project structure, logic, docs and code easily understandable. To this end use plain english, not technobabble"

**Locked:** First build is the backbone. Sim logic from the old spec stays. Data model is the thing we get right. Write so a person can follow it.

---

## 2026-08-23 — Two layers of looking, two runs, no mixed UI

> "A sounds good but we need a way to ensure [baking “one run” assumptions into storage] doesnt happen"

> "there should be a few layers of dashbaord/analysis here. Visualizing a sim: scrub through time kind of thing: proably a good use for a minimal mock dashbaord w a map and some charts. comparing sims: mostly a time series & metrics comparrisssion. This may be best done in a dedicated notebook. So this kind of does the best of A and B we should do a min of 2 sim runs, the dash can run one at a time and we dont have the mixed ui concern since the compare analysis is just a notebook not a whole app"

**Locked:** At least two runs. Dashboard plays one reality at a time (map + charts + scrub). Notebook compares. Storage must not assume there is only one run.

---

## 2026-08-23 — Each run is a reality

> "Yeah i think B too, each sim run is its own representation of reality. Each reality can reuse the data model we build"

> "There should be a good way to ensure the index accurately reflects what lives in the runs without having to update the index manually to reflect? At the very least a run spinup rule that instructs how to properly setup a run and record it in the index"

**Locked:** Folder per run. Index is generated by scanning folders, never typed by hand. A spinup rule tells agents how to open a run and get it into the index.

---

## 2026-08-23 — Trust a run without doing full uncertainty yet

> "yeah b, lets note c as a future aspiration in some kidn of doc though"

**Locked:** Every run gets a short card plus an automated checklist. Full uncertainty (many seeds, ranges, error bars) is a later note, not this build.

---

## 2026-08-23 — Map

> "a"

(Texas-ish fake lat/lon. Outages hit a region / cluster of points.)

**Locked:** Homes have made-up Texas coordinates and a region.

---

## 2026-08-23 — Types, a picture, and a database

> "I like a for its simplicity, from b i want a visual of the data model, that will help me understand it, from c i do want a db bc that will allow better serialization and would be easier for future expansion."

> "yeah thats right"

(on: types are law, each run folder holds one SQLite file, the picture is a view of those types/tables)

**Locked:** Types write the same SQLite shape in every run. The plane diagram is drawn from the types, not a second schema.

---

## 2026-08-23 — Clocks, and units that go offline (open)

> "on plane 3: the clocks/tics it does seem to me that we should have a market clock and hq clock and each unit has it's own internal clock. This could be a bit too complext but a central piece of the Base fleet is that units can go offline. this piece of real world fleet dynamics seems important to model here. this may have further implications we need to discuss."

> "Also we should be keeping a decision and intent log with my decisions and reasoning from these messages. These should use my exact language if possible and will form a sort of base for a future post"

**Locked:** This file exists. Clock / offline split is not locked yet — see the open question in the same day's design chat.

---

## 2026-08-23 — Don't decorate the cartoon

> "I like A, C is appealing for its realisim. but i think were reaching to validate an implicit model we've already latched onto instead of elaborating on a better model that could better reflect reality. Let's explore this."

**Locked:** A/B/C (all-online vs one silent burst vs full comms) are on hold. First ask whether HQ-dispatches-every-15s is even the right picture.

---

## 2026-08-23 — Local is default, HQ is a privilege

> "that seems good"

(on: local authority always owns reserve and the house; HQ is an overlay only when the link is up; unit reverts to local when silent; market clock still counts truth; no parallel HQ-SOC movie)

**Locked:** Two authorities + one market clock. Offline is “HQ not in the loop,” not a second physics. First runs can stay all-linked; the tables still record who decided.

---

## 2026-08-23 — Planes approved

> "yes"

(on the revised four planes: local default, HQ privilege, one truth movie, market clock, link column exists)

**Locked:** Section 1 is approved. Next: how a run is born.

---

## 2026-08-23 — Run birth approved

> "yea"

(on: folder + run.db, card inside the db, validate then create, snapshot pieces in, success only if checks pass, index is a scan)

**Locked:** Section 2 is approved. Next: tables in `run.db`.

---

## 2026-08-23 — Grain: don't conflate unit and place

> "home seems to conflate a unit and a location. A location is a static kind of thing. Could be linked to a user in the future, but probably too much for now. A location can have multiple units. Units can change locations."

> "price curve seems simplistic? perhapse we should have a more ercot like event?"

> "outage plan may need to drill down one level deeper than region, a substation can go down within a city or a hv transmission stepdown facility can go down and knock out a city"

> "homes will draw power based on fairly well known distributions as a function of time of day and time of year and or local temperature"

> "Make sure grain checks out"

> "Does this make sense given the dynamics of a power grid and what a company like base would intend to do w their fleet?"

**Locked:** Not yet — grain is being reworked. Users stay out. See design chat.

---

## 2026-08-23 — Grain approved

> "yea"

(on: location ≠ unit; install joins them; load is location × 5 min from TOD/season/temp; outage hits a grid node (substation / city / region); market event writes one 5-min series at one settlement point; tick is unit × 15s; link and island are at the location; users later)

**Locked:** Section 3 grain is approved. Tables get rewritten to match.

---

## 2026-08-23 — ERCOT events have components

> "ercot events have separate components"

**Locked:** Not yet — a single $/MWh series is too coarse. Design chat is splitting the event and the price.

---

## 2026-08-23 — AS is later

> "mark it as a future aspiration"

(on ancillary services)

**Locked:** First schema settles energy SPP parts only: energy, scarcity, congestion, losses. AS lives in `notes/later.md`.

---

## 2026-08-23 — Tables approved

> "looks good"

(on section 3, including SPP parts and AS-as-later)

**Locked:** Section 3 is approved. Next: the 15s loop.

---

## 2026-08-23 — Loop approved

> "yea"

(on the 15s step: hold 5-min price/load, island from outage node, link at location, local always, HQ only if linked and not islanded, no last-setpoint, settle SPP on 5-min boundary)

**Locked:** Section 4 is approved. Next: dashboard and notebook.

---

## 2026-08-23 — Looking approved

> "yes"

(on: dashboard one run at a time, map + charts + scrub; notebook compares two runs; no mixed UI)

**Locked:** Section 5 is approved. Next: repo layout and agent rules.

---

## 2026-08-23 — Repo list felt big

> "seems a bit expansive, do we really need all that?"

**Locked:** Not yet — cut the folder/skill sprawl. See design chat.

---

## 2026-08-23 — Thin repo, keep skills and the log habit

> "i like that, keep the skills though, keep the project log habit"

**Locked:** One Python package, scenarios, runs, dashboard, analysis, notes, spec. Skills stay. Daily project log stays. Then write the spec.

---

## 2026-08-23 — Spec approved

> "looks good"

(on `docs/superpowers/specs/2026-08-23-sim-backbone-design.md`)

**Locked:** Spec is the build spec. Next: implementation plan.

---

## 2026-08-23 — Execute on a branch, no worktrees

> "subagent, no worktrees, parallel or sequential based on what makes sense"

**Locked:** Subagent-driven on branch `sim-backbone`. Sequential implementers (tasks depend). No worktrees.

---

## 2026-08-23 — Fix the dashboard, parallel agents

> "look at the dashbaord yourself and identifyt issues"

> "spin up sub agents to rectify these issues"

**Locked:** Fix what we found: stride-by-row dropping units, linear lat/lon, clocks as minutes, picker hiding policy, charts/outages/load/layout, and a world polite enough that both runs look the same.

---

## 2026-08-23 — Stride by time, expose policy kind

> "Downsample by **time**, not row index. If you stride, keep **every unit** at each kept `t_s`."

> "trace_stride means \"keep every Nth timestamp\" (still 15s grid). Threshold stays 200_000 rows."

> "Add `last_tick_s` to `run_payload` (max t_s in the returned ticks)."

> "Read it from the `local_policy` table (`kind` column). Do not invent it. Existing rows use id `lp`; kinds are `fixed_reserve` or `aggressive`."

**Locked:** Serve strides every 4th timestamp once ticks exceed 200k rows, keeps every unit at those times, and reports `last_tick_s` plus `local_policy_kind` from the table.

---

## 2026-08-23 — Clusters, not a line; start SOC so the floor can bind

> "Lat and lon are perfectly correlated → diagonal of overlapping dots."

> "Start SOC is `0.5 * capacity` in `battery_fleet/loop.py`. Floor is 20% = 2 kWh. Scarcity is 10 min at 5 kW → leftover ~4.1 kWh. Floor never binds. Scoreboards match ($104.94, 0 floor)."

> "For `n_locations > 3` (the 60-site demos): two clusters, one per city (`c0` / `c1`)"

> "Default **0.5** so `tiny.yaml` and existing loop tests stay put."

> "Set `start_soc_frac: 0.25` on `scenarios/fixed_20.yaml` and `scenarios/aggressive.yaml` only."

**Locked:** 60-site worlds place two city clusters inside the Texas box. `start_soc_frac` lives on World/YAML, default 0.5; demos start at 0.25 so the 20% floor can bind.

---

## 2026-08-23 — Trash the old runs, new serve, check the map

> "ok trash the old runs, kill the proc spin a new one ensure the map renders correctly"

**Locked:** Delete the diagonal pair. Restart serve. Confirm the map is two clusters, not a line.

---

## 2026-08-23 — Actual map background

> "can we have an actual map background?"

**Locked:** Fleet dots sit on real map tiles (streets/place names), not a blank canvas.

---

## 2026-08-23 — Playback controls

> "ok and playback controls"

**Locked:** Play, pause, restart, and speed on the time scrubber. Not just a dead slider.

---

## 2026-08-23 — Mermaid from the types

> "yeah"

(on generating Mermaid from `types.py` so the picture lives in the repo)

**Locked:** Planes + ER view are generated from the dataclasses. Checked-in `data-model.md` must match. Hand-editing the picture is wrong.

---

## 2026-08-23 — Travis County, demand as a house curve

> "nix the current runs, move the loc to travis county area."

> "I'd like the unit demand to be a function of time of day, time or year, temperature, on top of a distribution of normal residential demand. that make sense?"

> "then i should be able to let the map show markers colored by demand or soc."

**Locked:** Trash the DFW run folders. Place sites in Travis County. Demand = house draw (not unit) from TOD + season + temp on a residential distribution. Map color mode (demand vs SOC) is next, not this change.

---

## 2026-08-23 — Three days, heterogeneous demand, legend

> "new sims, 3 day spans, add a legend for the map, yeah Heterogeneous demand is what i want"

**Locked:** Demo runs are 3 days. Each location draws from a house scale on a residential distribution, times TOD + HVAC(temp). Map legend + color by SOC or demand.

---

## Later (named, not built)

See `notes/later.md`. Short list: full uncertainty, ancillary services, users.

---

## 2026-08-29 — Snapshot then empty the tree

> "maybe do a commit but basically nuke it all after that. keep the venv, gitignore, but thats it"

**Locked:** Commit the current tree. Then delete everything except `.venv` and `.gitignore`.

