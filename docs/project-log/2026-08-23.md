# 2026-08-23

## Did

- Cut the old reserve-vs-revenue spec down to a sim backbone.
- Locked the data model: four planes, location ≠ unit, local default / HQ privilege, ERCOT price parts, folder + `run.db` + generated index.
- Wrote `docs/superpowers/specs/2026-08-23-sim-backbone-design.md` and the thin agent kit (rules + three skills).
- Spec approved. Wrote `docs/superpowers/plans/2026-08-23-sim-backbone.md`.
- Started `notes/intent-log.md` and `notes/later.md`.

## Broke

- nothing (no code yet)

## Learned

- The old spec’s 15s/5min loop was fine; baking “home” and “one price curve” would have been the real mess.
- “Each sim run is its own representation of reality. Each reality can reuse the data model we build.”

---

## Did

- Ran paired demo scenarios at n_locations=60, duration_s=86400: `fixed_20` (~36s) and `aggressive` (~36s); both success.
- Run ids: `20260823T2150450007` (fixed_reserve 20%), `20260823T2151240007` (aggressive).
- Scoreboards identical: revenue $104.94, shortfall 0, coverage 1.0, locations_dark 0, time_at_floor_s 0 for both.
- Added `analysis/compare.ipynb` (3 cells) and `tests/test_notebook_exists.py`; full suite 43 passed.

## Broke

- nothing

## Learned

- Fixed vs aggressive floor did not diverge scoreboards on this world — worth probing whether scarcity/outage window actually binds the reserve.

---

## Did

- Compare notebook died on `runs/index.json` because Jupyter cwd is `analysis/`; `Path("runs")` pointed at a folder that does not exist. The two demo runs were already under repo `runs/`.
- Notebook now uses `default_runs_dir()`. `load_index` returns `[]` if the folder is missing instead of trying to write `index.json` into nowhere.

## Broke

- `analysis/compare.ipynb` cell 1: `FileNotFoundError: runs/index.json`

## Learned

- The index is a scan of repo `runs/`, not of the notebook’s cwd.

---

## Did

- Locked placement: 60-site worlds are two city clusters (`c0` west, `c1` east) inside the Texas box, not a lat/lon diagonal. Tiny stays a small local cluster.
- Locked start SOC as a World/YAML knob. Default 0.5. Demos start at 0.25 so a 10 min × 5 kW scarcity can hit the 20% floor.
- Did not spin the 24h demos.

## Broke

- nothing

## Learned

- "Floor never binds. Scoreboards match ($104.94, 0 floor)." Starting at half-full left ~4.1 kWh after scarcity; the 2 kWh floor never had a chance.

---

## Did

- Three agents patched the dashboard lies: stride now keeps all 60 units, clocks are `18:04` not `1080:00`, picker names the policy, map is two city clusters, outage band on SPP.
- Spun `runs/20260823T2235230007` (fixed_reserve) and `runs/20260823T2236010007` (aggressive). They finally diverge: $60.69 / 11 kWh short / 15 dark / floor binds vs $104.94 / 0 / 0 / no floor.

## Broke

- Time at floor is unit-seconds. Fixed run shows `353:11:15` — a clock of a sum, not wall time.

## Learned

- The old pair matched because the world was too polite, not because the policies were the same. Drop start SOC to 25% and the 20% floor actually does something.

---

## Did

- Trashed the diagonal runs and the SW–NE pair. Spun `20260823T2240450007` (fixed) and `20260823T2241210007` (aggressive). Cities sit east–west at the same lat. Map uses equal degrees so it reads as two clusters, not a slash. Fresh serve on 8766.

## Broke

- nothing

## Learned

- Stretching lat and lon independently turns any two offset blobs into a canvas-filling diagonal. Same-lat cities + equal aspect fixed it.

---

## Did

- Fleet map now sits on Carto dark OSM tiles. Two clusters land on Grapevine / Southlake–Keller. Zoom and pan work.

## Broke

- nothing

## Learned

- A blank canvas with lat/lon dots is not a map. Streets and town names make the Texas box real.

---

## Did

- Trashed the Grapevine / Southlake pair. Sites now sit in Travis County: Austin (`c0`) and Pflugerville (`c1`). Settlement point is `HB_SOUTH`.
- Spun `20260823T2254370007` (fixed_reserve) and `20260823T2255140007` (aggressive). Scoreboards unchanged — geography moved, physics did not.
- Demand is still one formula for every house. Seed is unused. Map fill is SOC only.

## Broke

- nothing

## Learned

- "unit demand" is the house draw at the location. TOD + season + temp on a residential distribution is the next interesting lever; color-by-demand needs those rows in the payload.

---

## Did

- New pair is 3 days (Aug 15–17), event on day 2 evening. `20260823T2305170007` fixed_reserve, `20260823T2305210007` aggressive.
- House demand is now a scale (lognormal, seed) times TOD + HVAC(temp). 60 unique kW at midnight. Fat tail to 7.2 kW.
- Map legend: SOC / Demand, ramp, islanded, silent. Clocks print `d2 18:00`.

## Broke

- Time at floor is still a sum of unit-seconds shown as a clock. Three days made that number worse.

## Learned

- Same invert, same scarcity window — but once houses are not identical, aggressive finally shorts a couple of them (0.26 kWh, 2 dark). Heterogeneous demand is the thing that made the policies disagree about backup, not just about the floor.
