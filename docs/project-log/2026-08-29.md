# 2026-08-29

## Did
- Gave the new battery and installation code a home. Split unit vs site. Required scenario time; the installation stamps batteries when they attach.
- Offline means the network cannot deliver a new command. Last command still runs. Not “unit is dead.”
- Dropped solar from the installation. Grid power is house load plus battery power. “Do not charge from the utility” now means do not charge.
- Locked that physics with four pytest cases.
- Added a clocked loop: each tick sets house load, delivers any new commands, then steps. No controller yet. A tick with no command for a unit keeps the last one.
- Added `build_fleet`. Counts plus a seed, not a roster file. One hundred identical 10 kWh units on eighty Texas sites. Twenty houses get a second battery.
- Added a fleet tick. One command map for all units, load per house, then every site steps. The record keeps fleet totals plus per-site command, actual, soc, and reachable so later observability can write those rows. No store yet. Author: “keep in mind what data needs to be persisted to inform observability later on.”
- Added the even splitter. Target in watts, divide by reachable units, `run_fleet` delivers the map. Fleet record now stores the target and tracking error (commanded minus actual).
- Added single-tick `solve_dispatch`. Same map as the splitter. Price in dollars per megawatt-hour. Score is energy plus a $5 ancillary placeholder, minus wear, minus miss. The placeholder does not change the power choice. Author: “don't skip R_as use a placeholder.”
- Finished the part-2 target job. `allocate_target` fills reachable units up to charge or discharge headroom. Full packs get zero. A target bigger than the fleet clips. After the step, commanded matches actual. The even splitter stays as the dumb baseline.
- First dashboard. Persist a 24h run as `runs/<id>/run.json`, serve it locally on :8000, scrub it on a map with charts and a command log. Prices are the baked 96-tick curve, not a live ERCOT pull. First run id: `20260829-145442` (96 ticks, 80 sites, 100 units). Author wanted a public portfolio piece but “no need to deploy anything rn, just want to run locally.”
- Dashboard review: the chrome worked, the day was empty. Night prices now go negative so the fleet charges. Offline sits on the spike (60–68). Idle ticks no longer get the $5 ancillary. Inland site box + spacing, map legend, hover bring-to-front, collapsed setpoints, CHARGE/SPIKE/OFFLINE chips. New run: `20260829-153336`.
- Carto tiles were watermarking “API key needed.” Switched to Esri dark gray (no key). Sites are an Austin-area fleet (neighborhood centroids + jitter), not a Texas box. Run `20260829-154142`.

## Broke
- nothing

## Learned
- Offline and islanded are different. One flag cannot be both.
- Solar made `grid_charging_allowed` a lie: the battery cannot see the house meter, so it cannot tell panel charge from utility charge. Author: “what if we got rid of the solar component?”
- Generate once, then two processes: serve JSON on :8000, Vite on :5173 proxying `/api`. No host, no picker. That is enough to see the day.
